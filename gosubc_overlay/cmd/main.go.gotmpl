// Code generated by github.com/arran4/go-subcommand/cmd/gosubc. DO NOT EDIT.

package main

import (
	"fmt"
	"os"

	"{{.PackagePath}}/cmd"
)

var (
	version = "dev"
	commit  = "none"
	date    = "unknown"
)

// Provenance
var (
	GeneratorVersion = "{{if .Provenance}}{{ .Provenance.Version }}{{end}}"
	GeneratedAt      = "{{if .Provenance}}{{ .Provenance.Timestamp }}{{end}}"
	ProjectCommit    = "{{if .Provenance}}{{ .Provenance.ProjectCommit }}{{end}}"
)

func main() {
	root, err := NewRoot("{{.MainCmdName}}", version, commit, date)
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error: %v\n", err)
		os.Exit(1)
	}

	if err := root.Execute(os.Args[1:]); err != nil {
		if e, ok := err.(*cmd.ErrExitCode); ok {
			if e.Err != nil {
				fmt.Fprintf(os.Stderr, "Error: %v\n", e.Err)
			}
			os.Exit(e.Code)
		}
		fmt.Fprintf(os.Stderr, "Error: %v\n", err)
		os.Exit(1)
	}
}
